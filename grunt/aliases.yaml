default:
  - build

build:
  - clean:build
  - truecolors_less
  - less
  - ngtemplates
  - copy
  - browserify:build

pretest:
  - clean:test
  - clean:coverage
  - build
  - browserify:test
  - connect:test

test:
  - jshint:dev
  - pretest
  - protractor_coverage:local
  - posttest

teststack:
  - jshint:deploy
  - pretest
  - localstack
  - protractor_coverage:stack
  - localstack:stop
  - posttest

posttest:
  - makeReport
  - clean:test

package:
  - uglify
  - postcss
  - svgmin
  - filerev
  - filerev_replace
  - robots

deploy:
  - build
  - package
  - s3
  - cloudfront
